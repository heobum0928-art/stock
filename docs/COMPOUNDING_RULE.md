# 복리 규칙 (초안, 미적용) — 2026-09-05 작성

**상태: 사용자 요청("복리로 돈이 올라가야지")으로 작성. 2026-11-25 버전 2.0 판정이 "계속"으로
나오기 전에는 켜지 않는다.** `CAPITAL_LADDER.md`는 수정하지 않는다(그 문서의 "더 이상 수정
안 함" 원칙). 이 문서는 사다리 위에 얹는 별도 규칙이며, 켜는 결정은 사용자가 한다(CLAUDE.md 6항).

## 1. 왜 필요한가
현재 모든 엔진의 베팅 크기가 **금액 고정**이다.
- 원본 숏 건당 250 / 완화 숏 건당 80 (코드 상수 `FUT_MARGIN_PER_TRADE`)
- core_lev 캡 500 → 명목 1,000 고정, 초과분은 되판다
이익이 지갑에 쌓여도 다음 베팅에 들어가지 않는다. 복리가 구조적으로 없다.

## 2. 규칙 (지금 고정, 켤 때 바꾸지 않는다)
**매월 1일 09:00 KST**, 그 시점 **총자본 E**(선물지갑 잔고 + 마진 순자산, 미실현 제외)로 아래를 재계산한다.

| 엔진 | 공식 | 현재값 대응 (E≈1,917) |
|---|---|---|
| 원본 숏 건당 증거금 | E × 13% | 250 |
| 완화 숏 건당 증거금 | E × 4% | 80 |
| 원본 숏 엔진캡 | 건당 × 2 | 500 |
| 완화 숏 엔진캡 | 건당 × 4.4 | 350 |
| core_lev 캡 | E × 26% | 500 |
| `global_cap_usdt` | 위 캡들 중 최대값 | 500 |

- 비율은 **2026-09-05 현재 설정을 E로 나눈 값**이다. 새 위험을 더하는 게 아니라 지금 위험 수준을 비율로 고정하는 것.
- **내려갈 때도 같은 공식**. E가 줄면 베팅도 준다. 이게 10배 레버리지와 다른 점이며 이 규칙의 핵심이다.
- 재계산은 한 달에 한 번만. 중간에 손으로 올리지 않는다(사다리 원칙과 동일).
- `FUT_MARGIN_PER_TRADE`는 코드 상수라 봇 재시작이 필요하다. 매월 1일 재시작을 규칙의 일부로 둔다.

## 3. 안전장치 (사다리 서킷브레이커 그대로 상속)
- 단일 손절이 설계값(-40%)을 5%p 초과 → 캡 동결 + 원인조사
- 자산 고점 대비 -10% → 즉시 비율을 한 단계(원본 13%→10%, 완화 4%→3%, core 26%→20%) 낮추고, 고점 회복 전 복원 금지
- 일일 손실한도(`daily_loss_limit_usdt`)는 **금액 고정 유지**(사다리 문서의 Kimi 해석). 베팅이 커질수록 하루에 멈추는 빈도가 느는 게 설계다.

## 4. 켜는 조건
1. 2026-11-25 판정: 버전 2.0 누적 순손익 > 0
2. 그 시점 사용자가 "켠다"고 결정
3. 켜는 날의 E로 1회 계산 후 다음 달 1일부터 월 1회

## 5. 기대와 한계 (기록용)
- 2026-08-25~09-05 실측 페이스(하루 +4.3 USDT, 총자본 대비 월 ≈7%)가 진짜면 12개월 ≈2.2배, 24개월 ≈5배.
- 그 페이스의 건당 EV 신뢰구간은 아직 0을 포함한다. 복리는 엣지를 키우지 않는다. 엣지가 있을 때 그 결과를 키우고, 없을 때 손실도 키운다. 그래서 판정 뒤에 켠다.
