# 사전등록 — 조용한 급등 롱 모의봇 (quietpump_long)

**작성일 2026-08-26. 봇 가동 전에 작성했다. 순수 모의(주문 없음).**

---

## 0. 왜 이걸 하는가

`docs/PREREG_SWEEP_BINANCE.md` 봉인을 2026-08-26에 단 한 번 열었고, 후보 5개 중
**이것 하나가 7개 기준을 전부 통과**했다(would_change_log 2026-08-26 (2)).

| | 홀드아웃(195종목, 미열람 데이터) |
|---|---|
| 건당 평균 | **+3.522%** (명목 기준, 레버리지 1배) |
| 기준선(무조건 롱 48h) | -0.347% |
| **우위** | **+3.87%p** |
| 부트스트랩 95% CI | [+1.09, +5.99] — 0 제외 |
| p | 0.004 |
| 월별 부호일치 | 9/12 |
| 2025-10 제외 | +3.416% (2025-10만 봐도 +4.16, 전멸 아님) |
| 표본 | 10,030건 / 159종목 / 351일 |

**이 프로젝트에서 봉인을 통과한 첫 후보다.** 그리고 지금까지의 모든 전략과 **방향이 반대(롱)**다.

### 그러나 백테스트일 뿐이다 — 미리 적어두는 우려
1. **승률 46.6%, 중앙값 -1.415%.** 절반 이상 지고 평균만 양수인 구조다.
   오늘까지 반복 확인한 "평균은 좋은데 중앙값·승률은 나쁜" 패턴에 정확히 해당한다.
2. 2026-08-24 업비트 스윕에서도 봉인을 통과한 후보가 있었으나 바이낸스로 옮기니
   **-16.56%로 무너졌다.** 백테스트 통과가 실거래 생존을 뜻하지 않는다.
3. 백테스트는 **손절·트레일링 없이 48h 고정 보유**로 계산됐다. 실거래엔 청산 규칙이 필요한데
   그걸 붙이는 순간 다른 전략이 된다.

---

## 부칙 (2026-08-29) — 보유기간 24h. **이 검정은 봉인검증의 효력을 상속하지 않는다**

**발견**: 이 문서와 원본 봉인검정은 **48시간 보유**인데, 실제 봇
(`scripts/quietpump_long_paper.py:66`)은 **처음부터 `HOLD_H = 24`**였다.
2026-08-26 가동 이래 쌓인 표본은 전부 24시간판이다. 문서가 코드를 서술하지 못했고,
나흘간 아무도 몰랐다.

**결정(사용자)**: 48h로 되돌리면 표본이 절반 속도로 쌓여 너무 느리므로 **24h로 간다.**
트레이드오프(아래)를 설명한 뒤 받은 결정이다. 코드는 변경 없음, 문서를 실제에 맞춘다.

**이 결정의 대가 — 반드시 판정문에 같이 적을 것:**

1. **0절의 봉인검정 수치(+3.522%, CI [+1.09,+5.99], p=0.004, 월별 9/12)는
   이 봇의 근거가 아니다.** 그것은 48h 고정보유로 계산된 값이다. 보유기간을 절반으로
   바꾸면 다른 전략이며, 24h판의 홀드아웃 성적은 **계산된 적이 없다.**
   → 앞으로 이 봇을 "봉인 통과한 유일한 후보"라고 부르지 않는다.
   그 문장은 48h판에만 해당한다.
2. 따라서 60쌍 판정은 **백테스트 뒷받침 없는 단독 검정**이 된다. 4절 기준
   (평균>0, 부트스트랩 CI 하한>0, 최대기여일 제외 부호유지, 국면 2개)은 그대로 두되,
   통과해도 "재현됐다"가 아니라 "이 표본에서 처음 관찰됐다"로만 기록한다.
3. 기존 24h 표본(2026-08-26~08-29, 신호 30쌍)은 **계속 사용한다** — 규칙이 바뀐 게
   아니라 문서가 코드를 따라간 것이므로 표본 연속성은 유지된다.

**하지 않을 것**: 24h 성적이 나쁘게 나온 뒤 "원래 48h였으니 그걸로 다시"라고 하는 것.
그것이야말로 CLAUDE.md 5항(여러 기준 대보고 통과한 것 고르기)이다.
48h판을 시험하려면 **별도 사전등록**으로 새로 시작한다.

**미해결로 남는 것**: 왜 코드가 24h였는지는 규명하지 못했다(작성 시 오기로 추정).
같은 사고를 막으려면 사전등록 문서와 코드 상수를 대조하는 절차가 필요하다 —
`docs/BOT_REGISTRY.md` 갱신규칙에 추가 검토.

---

## 1. 규칙 (지금 고정, 변경 금지)

원본 스윕(`research/m5bt/bsweep_volume.py:105-110`)의 정의를 **그대로** 쓴다.

| | 값 |
|---|---|
| 유니버스 | **바이낸스 USDT 무기한선물** 전체 |
| 신호 | `qv / median(qv, 288봉) <= 1.2` **그리고** `(종가/12봉전 종가 - 1)×100 >= 5.0` |
| | (= 24시간 거래대금 중앙값 대비 현재 거래대금이 1.2배 이하 = "조용" **그리고** 최근 1시간 +5% 이상) |
| 방향 | **롱** |
| 보유 | ~~48시간 고정~~ → **24시간 고정** (2026-08-29 부칙 참조) |
| 손절 | **없음** ← 백테스트와 조건을 맞추기 위해 |
| 트레일링 | **없음** |
| 레버리지 | **1배** (명목 기준 기록) |
| 쿨다운 | **12시간 / 코인** |
| 비용 | 편도 0.06%(수수료+슬리피지) 왕복 차감 |
| 펀딩 | **실측 반영** — 롱은 양수 펀딩 **지불** |
| 폴링 | 5분 |
| 동시 보유 | 최대 10건 |

**손절이 없는 것은 의도다.** 백테스트가 그 조건이었다. 실거래로 갈 경우 청산 규칙을
별도 설계·검증해야 하며, 그건 이 검정의 대상이 아니다.

## 2. 내장 대조군 — 이게 핵심이다

**신호 진입 1건마다, 같은 시각에 무작위 코인 1건을 동시에 모의 진입한다.**
(유니버스에서 균등 추출, 쿨다운·보유·비용 규칙 동일, 방향도 롱)

→ 시장 국면이 통째로 오르내려도 **짝이 맞는 대조군**이 생긴다.
백테스트의 기준선(무조건 롱 -0.347%)에 해당하는 것을 실시간으로 만든다.

## 3. 기록
`data/quietpump_long_trades.csv` — 진입/청산 시각, 코인, 진입가, 청산가,
신호값(qv비율, 1시간 수익률), 가격손익, 펀딩, 수수료, 순손익,
`kind`(signal | random), 신호 시각과 진입 시각의 지연(초).

## 4. 판정 기준 (결과 보기 전 확정 — 이후 수정 금지)

주지표: **건당 순수익률, 명목 기준 %** (가격 + 펀딩 + 수수료)

### 채택 후보 조건 — **전부** 충족
1. **독립 표본 60건 이상** (12h 쿨다운 적용 후, signal 기준)
2. (신호 평균) − (무작위 평균) **> 0**
3. **일-블록 부트스트랩** 4,000회 95% CI 하한 **> 0**
4. **최대 기여 단일 날짜 제외 시 부호 유지**
5. **서로 다른 시장 국면 2개 이상**에서 부호 동일
   (국면 = BTC 30일 수익률 강세>+10% / 횡보 / 약세<-10%)

### 즉시 기각
- 신호 − 무작위 **≤ 0**
- 60건 미달 상태로 판정 시도 → 기각이 아니라 **연장**

### 통과해도
**실거래 아님.** 사용자가 직접 결정·설정한다(`CLAUDE.md` 6항).
손절 없는 설계이므로 실거래 전 청산 규칙을 별도 설계·검증해야 한다.

## 5. 마감일 — 지금 확정
**2026-10-26 (2개월).** `docs/DEADLINES.md` 운용규칙 5("마감일 없는 데이터 수집은
시작하지 않는다")에 따라 미리 정한다.

| 조건 | 판정 |
|---|---|
| 60건 도달 + 위 5개 충족 | **채택 후보** — 사용자에게 보고, 실거래 여부는 사용자 결정 |
| 60건 도달 + 미충족 | **기각.** 임계값·보유기간 변형으로 이어가지 않는다 |
| 60건 미달 | "이 속도로는 판정 불가"로 기록하고 계속/중단을 그 자리에서 결정 |

## 6. 소량 실전 파일럿 — 별도 조기 체크포인트 (2026-08-28 추가)

**이것은 5항의 "채택" 기준과 다른, 더 낮은 문턱이다.** 60건·국면 2개를 다 채우기 전에도
**소량으로만** 실거래를 시작할지 여부를 가르는 별도 판정이다. "채택"과 "소량 파일럿"을
혼동하지 않는다 — 파일럿 시작이 채택을 뜻하지 않고, 파일럿에서 나빠도 채택 절차 자체가
무효가 되는 것도 아니다(표본이 다르므로).

**전제조건 (날짜와 무관하게, 먼저 충족돼야 함)**: 지금 이 전략은 손절 없이 24시간
고정 보유다(1항). 소량이라도 실거래에 넣기 전에 **청산 규칙을 별도로 설계·백테스트·
사전등록**해야 한다 — 이 문서의 60건 기준은 "손절 없는 버전"을 검증하는 것이므로,
손절을 붙이는 순간 다른 전략이 되고 그 결과는 아직 없다. 이 사전등록 없이는
날짜가 와도 파일럿을 시작하지 않는다.

**날짜: 2026-09-19.** `docs/DEADLINES.md`의 margin_short 존속판정 마감과 같은 날로
묶는다 — 별도 캘린더를 늘리지 않기 위함. 그 시점 표본(현재 페이스로 약 35~45건 예상,
60건 미달 가능성 높음)으로 판정하며, **부족해도 날짜를 미루지 않는다**(운용규칙과 동일).

| 그 시점 상태 | 판정 |
|---|---|
| 신호 − 무작위 우위 유지 (부호 양수) **그리고** 청산 규칙 사전등록 완료 | **소량 파일럿 시작** — 액수·설정은 사용자가 직접 결정(`CLAUDE.md` 6항) |
| 신호 − 무작위 우위 유지 **그러나** 청산 규칙 미비 | 파일럿 보류 — 청산 규칙부터 완성 후 재판정 |
| 신호 − 무작위 우위 사라짐/역전 | 파일럿 없음. 다만 5항의 60건·10/26 채택판정 자체는 별도로 계속 |

**이 체크포인트는 5항의 마감일(2026-10-26, 60건 완전판정)을 대체하지 않는다.**
9/19에 소량 파일럿을 시작했더라도, 10/26에 60건·국면2개 기준으로 별도로 "완전 채택"
여부를 다시 판정한다.

## 7. 이 문서가 막는 것
- 결과를 보고 임계값(1.2배, +5%)이나 보유기간(48h)을 바꾸는 것
- 60건 전에 "좋아 보인다"로 조기 판정하는 것
- 무작위 대조군을 빼고 절대 성적만 보는 것
- 통과했다고 바로 실거래로 넘기는 것
- 손절을 슬쩍 붙여놓고 "같은 전략"이라고 하는 것
- **청산 규칙 사전등록 없이 6항의 소량 파일럿을 시작하는 것**

## 8. 사전 선언
**나는 통과를 반반으로 본다.** 홀드아웃이 탐색보다 좋았고(+1.898 → +3.522) 7개 기준을
전부 통과한 것은 이 프로젝트에서 처음이다. 다만 승률 46.6%라 60건에서는 운의 폭이 크다.
이 예측은 판정 기준이 아니라 기록용이다.

## 9. 관련
`docs/PREREG_SWEEP_BINANCE.md`, `docs/would_change_log.md` 2026-08-26 (2),
`research/m5bt/bsweep_volume.py`, `research/m5bt/open_seal2.py`
