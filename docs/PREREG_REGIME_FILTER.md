# 사전등록 — 변동장 필터(BTC 24h변동 > 3.26% 시 신규진입 차단) 재검증

**작성일 2026-08-25. 계산 전에 작성했다.**

---

## 0. 왜 재검증하는가
사용자 질문("STORJ는 왜 안 들어감") → 2026-08-25 01:50~02:05 STORJUSDT가 신호(7h +31~38%,
거래대금 통과, 현물·선물 모두 상장)에 걸렸으나 **BTC 24h 변동 4.34% > 3.26%**로 차단됐다.

이 필터는 2026-08-07에 도입됐고 근거는 `margin_short_trader.py:268` 주석에 있다:
> 155건 백테스트 레짐분석 — 평온장(≤중앙값) 승률70.9%·평균+8.36% vs
> 변동장(>중앙값) 승률59.3%·평균+0.04%

**근거가 약하다:**
1. **그룹 비교**다(`CLAUDE.md` 3항이 금지한 방식은 아니지만 — 레짐은 처치가 아니라 신호의
   속성이라 짝비교가 원리적으로 불가능하다 — 그래도 가장 약한 형태의 증거다)
2. **문턱 3.26%가 그 표본의 중앙값**이다. 표본 안에서 고른 값이라 과적합 소지가 있다
3. n=155

**차단 규모**: 2025-08~2026-07 전체 시간의 **43.4%**를 차단한다(월별 9.0%~81.2%).

## 1. 검정할 것 — 정확히 하나
`base_real.npz`의 **신호 2,399건 전체**에 대해 진입 시각(t0)의 BTC 24h 고저폭을 계산하고
**3.26%를 기준으로 둘로 나눈다.** 문턱은 **현행 값 하나만** 쓴다.
다른 문턱(2.5/3.0/4.0)·다른 지표(ATR·실현변동성)를 같이 보면 이 문서는 무효다.

- 데이터: `research/m5bt/pq/BTCUSDT.npz` 롤링 288봉 고저폭, `base_real.npz`의 `ret_opt`(펀딩 포함)
- 단위: **건당 순수익률, 증거금 기준 %**
- 홀드아웃: 기존 정의 `md5(sym)%4==0`

## 2. 판정 기준 (지금 고정 — 결과 보고 변경 금지)

주가설: **변동장 신호가 평온장 신호보다 나쁘다.**

### 필터 유지 조건 — **전부** 충족
1. (변동장 평균) < (평온장 평균)
2. 차이의 **종목 클러스터 부트스트랩** 4,000회 95% CI가 **0을 포함하지 않음**
3. **홀드아웃에서도 부호 동일**
4. **2025-10 제외 시에도 부호 유지**
5. **월별 안정성**: 12개월 중 **8개월 이상**에서 부호 동일

### 하나라도 미달이면
**"근거 불충분"으로 기록한다.** 그렇다고 자동으로 제거하지 않는다 —
현행 전략은 두 국면 모두 기대값이 음수이므로 **어떤 필터든 거래를 줄이면 손실이 준다.**
제거는 별도 판단이며 `CLAUDE.md` 6항에 따라 사용자가 결정한다.

## 3. 병기 필수
- 두 국면 각각의 건수·평균·승률·표준오차
- **차단된 43.4%가 실제로 몇 건인지** (시간 비율 ≠ 신호 비율)
- 필터 적용 시/미적용 시 **총손실 합계** (건당 평균이 아니라 총액 — 2026-08-25 (1)의 함정)

## 4. 사전 선언
**나는 "차이는 있으나 기준 5개를 다 통과하지는 못한다"를 예상한다.**
근거: 155건에서 나온 8.36%p 차이는 건당 SD 43.74%p 대비 크지 않다
(n=155면 최소감지 6.89%p, 경계에 걸친다). 기록용이며 판정 기준이 아니다.

## 5. 관련
`docs/would_change_log.md` 2026-08-25, `scripts/margin_short_trader.py:268`
