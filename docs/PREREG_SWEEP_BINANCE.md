# 사전등록 — 바이낸스 1년 전수 특징 탐색 (오토리서치 2차)

**작성일 2026-08-24. 데이터를 열어보기 전에 작성했다.**

---

## 0. 1차(업비트) 스윕에서 배운 것 — 이번에 고치는 것

1차 스윕(`PREREG_FEATURE_SWEEP.md`)은 업비트 5분봉 5개월을 **시간 기준 60/40**으로 나눴다.
후보 4가 봉인을 통과했으나, 바이낸스 1년으로 재검증하니 **2025-10 한 달(551건, -37.19%)이
전체를 죽였고** 나머지 11개월은 -0.50%로 거의 0이었다(2026-08-24 (13)).

> **구조적 결함: "최근에만 되는 현상"은 시간분할 홀드아웃을 그대로 통과한다.**
> 탐색 구간과 봉인 구간이 둘 다 최근이면, 그 시기에만 통하는 것을 걸러낼 수 없다.

**이번 설계는 그 결함을 정면으로 막는다.**

| | 1차 (업비트) | **2차 (바이낸스)** |
|---|---|---|
| 기간 | 5개월 | **1년** |
| 대폭등 구간 | 없음 | **2025-10 포함** |
| 생존편향 | 있음(상폐 종목 없음) | **없음(상폐 276종목 포함)** |
| 홀드아웃 | 시간 분할 | **종목 분할** |
| 월별 안정성 요구 | 없음 | **있음 — 핵심 추가** |
| 펀딩비 | 없음(현물) | **실측 반영** |

## 1. 데이터
- `research/m5bt/pq/` — 바이낸스 USDT 무기한선물 **5분봉 805종목**,
  2025-08-01 ~ 2026-07-31, **상장폐지 276종목 포함**
- 펀딩: `research/m5bt/fund/` 실측
- **홀드아웃 = `md5(symbol) % 4 == 0`** (기존 정의 그대로, 약 25% 종목).
  탐색은 나머지 75% 종목에서만 한다.

⚠️ 이 데이터는 지금까지 약 2,800회 검정에 쓰였다. 다만 그 검정들은 전부
**"7h +30~40% 급등"이라는 하나의 신호집합(2,399건)**을 대상으로 했다.
이번은 **전 봉을 대상으로 한 특징 전수 탐색**이라 대상이 다르다.
그래도 같은 가격 데이터이므로 **판정 기준을 1차보다 엄격하게** 둔다.

## 2. 예측 대상
진입 = 신호 봉의 **다음 봉 시가**(look-ahead 방지). 청산 = **고정 보유**(스탑·트레일링 없음).
지평 `1h / 4h / 12h / 24h / 48h`. **롱·숏 양쪽** 모두 평가.
비용 편도 **0.06%** 왕복 차감. **펀딩 실측 반영**(숏은 양수 펀딩 수취).
명목가 기준 %로 보고(레버리지 1배).

## 3. 탐색 단계 (자유)
특징을 기계적으로 대량 생성한다. 개수 제한 없음 — 그것이 홀드아웃을 봉인하는 이유다.
**탐색은 비홀드아웃 종목(75%)에서만.** 홀드아웃 종목은 후보 확정 전까지 건드리지 않는다.

## 4. 후보 선정 — 정확히 **5개**

선정 기준(지금 고정, 결과 보고 변경 금지):
1. 탐색 구간 **비용·펀딩 차감 후 평균 > 0**
2. 표본 **500건 이상**, **종목 30개 이상**, **날짜 30일 이상** ← 1차에 없던 조건
   (1차에서 후보2가 코인 6개·9일로 통과해버린 결함을 막는다)
3. 신호 밀도 **10% 미만**
4. **월별 안정성**: 12개월 중 **8개월 이상에서 부호 동일** ← **핵심 추가**
5. 위를 만족하는 것 중 평균 상위 5개, **계열당 최대 2개, 특징당 지평 1개**

## 5. 판정 기준 (홀드아웃, 결과 보기 전 확정)
후보 5개 각각 **전부** 충족해야 채택 후보다.
1. 홀드아웃 평균 **> 0**
2. **일-블록 부트스트랩** 4,000회 95% CI 하한 **> 0**
3. **BH-FDR 보정**(5개 동시, q=0.10) 통과
4. **최대 기여 단일 날짜 제외 시 부호 유지**
5. 탐색 구간과 홀드아웃의 **부호 동일**
6. **홀드아웃에서도 월별 8/12 이상 부호 동일** ← 추가
7. **2025-10을 제외해도 부호 유지**, 그리고 **2025-10만 봤을 때 -20% 미만이 아닐 것**
   (대폭등 구간에서 전멸하는 전략을 걸러낸다)

### 사전 선언
**0개 통과가 기본 예상이다.** 1차에서 통과한 1개도 결국 무너졌다.
0개면 "이 데이터에서 비용을 넘는 규칙을 찾지 못했다"로 기록하고 종료한다.

## 6. 봉인 규칙
홀드아웃은 **후보 확정 후 단 한 번** 계산한다. 결과를 보고 후보를 바꾸거나 추가하지 않는다.
두 번째로 열면 결과를 보고하지 않고 이 문서를 폐기한다.

## 7. 하지 않을 것
- 홀드아웃을 보고 특징 조정 / 후보 5개 초과
- 출구 규칙(스탑·트레일링) 동시 최적화
- 통과 못 했을 때 "다른 지평·다른 비용가정으로 다시"

## 8. 통과해도 실거래 아님
페이퍼 → 독립 60건 + 국면 2개 → 사용자가 직접 결정·설정(`CLAUDE.md` 6항).

## 9. 관련
`docs/PREREG_FEATURE_SWEEP.md`(1차), `docs/would_change_log.md` 2026-08-24 (9)(12)(13)
