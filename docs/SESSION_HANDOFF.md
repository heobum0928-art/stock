# 세션 인수인계 — 2026-08-24 기준

**이 문서는 다음 세션이 맨 처음 읽는 것이다.** `CLAUDE.md`와 함께 본다.
상세 이력은 `docs/would_change_log.md` (append-only), 봇 현황은 `docs/BOT_REGISTRY.md`.

---

## 1. 지금 상태 (한눈에)

| 항목 | 값 |
|---|---|
| 실거래 완료 | **50건** (+ 오픈 2건) → **51건 관문까지 1건** |
| 열린 포지션 | SCUSDT, SPKUSDT (2026-08-24 기준 둘 다 이익 중) |
| 누적 실현손익 | **-47.82 USDT (약 -6.6만원)** |
| 그림자함대 | 종료 127건 / HOLD24 짝비교 **25/60건** |
| 실거래 봇 | `margin_short_trader.py`(숏) + `core_leveraged.py`(BTC 롱) — **둘 다 가동 중** |
| 파라미터 | **전부 동결** (51건 관문) |
| 증거금 | 50 USDT × 5슬롯 |

**월 손실 실측**: 31.5일 / 50건 = 월 48건 페이스. 건당 -0.956 USDT →
**월 약 -45 USDT (6.3만원)**. 모델 추정(-3.33%/건)으로는 월 -79 USDT (10.9만원).

---

## 2. 이 프로젝트의 현재 결론 (2026-08-22~24 검증 결과)

### 핵심: 진입 신호에 정보가 없다

현행 전략 = "7시간 안에 30~40% 급등한 알트를 2배 숏".
5분봉 1년 2,399신호(805종목, 청산 시뮬·펀딩 실측·비용 반영):

| | 값 |
|---|---|
| 신호 진입 (숏) | **-4.82%** |
| **무작위 진입 (숏)** | **-1.22%** |
| 초과분 | **-3.60%p** |

**신호가 무작위보다 나쁘다.** 정합 후 진짜 기대값 추정 **-3.33%/건, 95% CI [-5.13, -1.59]**.

실거래가 좋아 보였던 이유는 규명 완료 — **격차의 81%가 소표본 운**, 나머지는
"봇이 5분마다 종가만 봐서 봉내 꼬리에 스탑이 안 걸리는" 관측모델 효과인데
2026-08-07 서버측 STOP_MARKET 도입으로 **지속 불가능**.

### 검증한 개선 후보 8개 중 7개 기각

| # | 후보 | 결과 |
|---|---|---|
| 1 | 진입조건 완화(하한 10~25%, 상한 해제) | ❌ 480조합, 청산+슬롯 반영 시 양수 0개 |
| 2 | 단계적 청산(부분청산) | ❌ 111검정 유의 0건 |
| 3 | 손절선 조정(-20/-25/-30) | ❌ 전부 -40%(현행)보다 나쁨 |
| 4 | 메이저 코인 숏 | ❌ 1,808검정 양방향 유의 0건 |
| 5 | 펀딩비를 진입 신호로 | ❌ 단일 이상치 1건(HFT)이 스프레드의 86% |
| 6 | 국면 조건부(횡보장 제외) | ❌ 243검정 유의 0건. **인용한 표가 재현 불가였음** |
| 7 | 부호 뒤집기(같은 신호에 롱) | ❌ 이득이 전부 펀딩. 가격만 +0.07%, 무작위 롱 대비 p=0.50 |
| 8 | 국면 × 방향 교차 | ❌ 81검정 "양수+유의" 0건. 국면 기여 +0.54%p(p=0.809) |
| **9** | **익절/트레일링 파라미터** | ⬜ **미실시 — 유일하게 남은 후보** |

**⚠️ 5·6·7·8이 전부 같은 형태로 죽었다: 단일 날짜/단일 이상치 산물.**
특히 **2025-10-11 하루**가 반복 등장한다 — 전체 데이터의 11.9%(285신호),
횡보장 손실의 51%, 롱 이익의 대부분. **그날은 무작위 진입도 숏 -21.55% / 롱 +24.74%**로
신호와 무관한 시장 전체 사건이다.

**오늘 총 검정 약 2,800회.** 새 가설을 계속 만들면 우연히 하나 걸린다.
**남은 1개(익절) 이후로는 새 슬라이싱 가설을 만들지 않는 것이 원칙.**

---

## 3. 다음에 할 일 (순서)

### 즉시
**익절/트레일링 파라미터 검증** — 마지막 후보.
- 근거: 실거래 48건에서 MFE 합계 +937.6% vs 최종 -66.1% = **반납 1,003.6%p**.
  MFE 15~25% 도달한 20건이 최종 평균 **-1.01%**로 마감.
- 현행 트레일링은 **+15% 도달해야 켜지고 10%p 반납해야 나간다**. 도입(2026-08-10) 후
  파라미터를 한 번도 검증·최적화한 적이 없다.
- ⚠️ **48건으로 8개 익절선을 시험한 결과(+15%에서 부호 전환)는 과적합이므로 근거로 쓰지 말 것.**
  +20%는 -7.34%로 급락 — 곡선이 불안정하다.
- ⚠️ **반납 벤치마크 필수**: 무작위 진입도 비슷한 MFE 반납을 보이면 1,003%p는 정보량 0이다.
  이 벤치마크 없이는 시작하지 말 것.
- 검증 조건: 5분봉 1년 2,399신호(48건 아님), 청산 시뮬, 슬롯5, 홀드아웃,
  다중검정 보정, 무작위 기준선, **단일 날짜 의존도 검사**.

### SC 또는 SPK 청산 시 (51건 도달)
1. `python scripts/gate_eval.py` 실행, **출력을 그대로** 판정문에 기록
2. **사전확정 잣대(CSV)로 판정한다.** 원장 잣대로 바꾸지 않는다 —
   6개 AI 만장일치. 원장 결과는 병기(스크립트가 자동 출력).
3. 판정문에 명시: "공식 잣대(CSV)에 펀딩비 미포함 결함이 2026-08-23에 발견됨.
   원장 잣대로는 3/4이나 원장은 건별 오염(12h 내 재거래 매칭 오류) 존재.
   **두 잣대 모두 결함이 있어 판정 신뢰도 제한적.**"
4. **거래는 계속된다.** 51건은 "파라미터를 바꿔도 되는 자격"이지 성적표가 아니다.

### 약 9일 뒤 (짝비교 60건)
`docs/PREREG_HOLD24.md` 사전등록대로 판정.
⚠️ **전망 안 좋음**: 짝차이가 1~15건 +9.35%p → 21~25건 **-4.49%p**로 역전됐고
최근 10건 평균 **-2.23%p**. 부호검정 8:8, **p=1.000**. 기각 가능성이 높다.

### 2026-09-02
그림자함대 6변형 판정. **6개 AI 만장일치로 "그날까지 기다려라"** — 조기 판정 금지.
- 주변평균 폐기, **짝비교만** 사용
- **레짐 편향 반드시 명시**: 표본이 전부 BTC 강세장 한 구간
- V3_fund8h는 신호 35건 중 34건 스킵 → 표본 부족이 아니라 **설계 실패**로 결론
- Atom 제안: 9/2는 채택/기각이 아니라 **기술 보고일**로 두고, V2는 60건 도달 시 별도 판정

### 2026-09-19
`DEADLINES.md` 부칙3에 따라 **판정일 → 점검일로 격하됨**.
필요표본 220건인데 그때까지 60~70건이라 "미결정"이 이미 예정돼 있다.

---

## 4. 미해결 항목

| 항목 | 내용 |
|---|---|
| **틱 데이터 검증** | 봉내 체결가 불확실성. 결과가 -0.55% ~ -2.36% 밴드로 흔들린다 |
| **CSV 가격원 혼합 버그** | 진입가는 선물 실체결가, **청산가는 현물 티커**. 스탑·트레일링 판정도 현물가. 원장과 건당 최대 88%p 괴리. **포지션 없을 때 수정할 것** (진행 중 수정하면 손절선이 도중에 바뀜) |
| CSV 펀딩비·수수료 미기록 | 건당 -3.31%p 누락. 원장 잣대로 우회 가능 |
| 원장 orderId 매칭 | 12h 내 재거래 시 손익 귀속 오류. 현재 6묶음 14건 |
| 블랙리스트 규칙 | "연속 2회 스탑"이 승리로 리셋됨. TUT는 3패 -82.4 / 2승 +9.5인데 스트라이크 1. **누적 손익 기준으로 변경 검토**(51건 이후, 사후편향 경고 병기) |
| 롱 전략 | 여전히 미해결. 4번 실패(모멘텀·평균회귀·급락반등·부호뒤집기) |

---

## 5. 사용자 결정 사항 (변경 금지, 사용자 확인 없이 뒤집지 말 것)

- **실거래 계속, 증거금 50 유지.** 6개 AI 만장일치 중단 권고와 정정된 비용
  (월 6~11만원)을 확인한 뒤 내린 결정.
- **목표는 손실 회피가 아니라 수익 극대화.** 다만 "변동성이 크다"와
  "기대값이 음수다"는 구분해서 말할 것.
- 롱+숏 병행이 프로젝트 목표.

---

## 6. 2026-08-24 세션에서 AI(나)가 낸 오류 — 재발 방지용

같은 실수를 반복하지 않기 위해 기록한다. `CLAUDE.md`에 규칙으로 반영돼 있다.

1. **단위 오독 2회** — `pnl_pct`는 명목가 기준(증거금은 2배). 원장
   `funding_usdt/margin_usdt`는 **이미 증거금 기준**인데 2를 또 곱했다.
2. **짝비교 대신 그룹평균 비교** — V5_stop30 vs V0_base에서 부호가 반대로 나왔다.
   프로젝트 통틀어 3번째 재발.
3. **다른 데이터셋의 표를 재현 없이 인용** — 국면별 표(728종목 런)를 801종목
   결과인 줄 알고 사용자에게 보고. 재현하니 부호 반전이 존재하지 않았다.
4. **월 손실 3~4배 오산** — 월 12건으로 가정(실제 48건). "월 2~3만원"이라 보고했으나
   실제 6~11만원. 사용자가 이 수치로 판단할 뻔했다.
5. **"이익 확대는 한 번도 안 봤다" 과장** — 트레일링이 2026-08-10에 바로 그
   문제로 도입됐고 반사실 검증도 완료돼 있었다.
6. **"9/2는 형식이다"** — 6개 AI가 전원 "기다려라"라고 한 것과 반대로 말했다.
7. **통과하는 기준 골라내기** — 같은 48건에 평균/중앙값/승률 3개를 대보고
   통과한 둘을 보고. 사용자가 "과적합 아니냐"로 지적.
8. **단일 이상치 기반 가설 제시** — 펀딩비 가설. 사전등록된 이상치 제외 규칙을
   적용했으면 부호가 반전돼 애초에 제시하지 않았을 것.

**패턴**: 계산 직후 검산 없이 곧바로 해석·보고한다. 세션이 길어질수록,
데이터셋이 여러 개 돌아다닐수록 빈도가 는다.
