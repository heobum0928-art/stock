<UserControl x:Class="EdgeInspector.Views.SegmentationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:EdgeInspector.Views">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>
    <DockPanel LastChildFill="True" Background="{StaticResource ContentBg}">

        <!-- 가이드 헤더 -->
        <Border DockPanel.Dock="Top" Padding="20,14,20,10">
            <StackPanel Orientation="Horizontal">
                <Border Background="{StaticResource Success}" CornerRadius="4" Padding="7,2" VerticalAlignment="Center">
                    <TextBlock Text="세그멘테이션" Foreground="White" FontSize="11" FontWeight="SemiBold" />
                </Border>
                <TextBlock Text="에지 세그멘테이션" Style="{StaticResource H1}" FontSize="18" Margin="12,0,0,0" VerticalAlignment="Center" />
            </StackPanel>
        </Border>

        <!-- 툴바 -->
        <Border DockPanel.Dock="Top" Background="{StaticResource SidebarBg}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,1" Padding="16,9">
            <StackPanel Orientation="Horizontal">
                <Button Style="{StaticResource SecondaryButton}" Click="OpenModel_Click" Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE81E;" FontFamily="Segoe MDL2 Assets" FontSize="14" VerticalAlignment="Center" Margin="0,0,7,0" />
                        <TextBlock Text="모델 열기" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource PrimaryButton}" Click="OpenImage_Click" Margin="0,0,20,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE8E5;" FontFamily="Segoe MDL2 Assets" FontSize="14" VerticalAlignment="Center" Margin="0,0,7,0" />
                        <TextBlock Text="이미지 열기" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
                <CheckBox Content="오버레이 표시" IsChecked="{Binding ShowOverlay}" VerticalAlignment="Center" Margin="0,0,20,0" />
                <TextBlock Text="Threshold" VerticalAlignment="Center" Style="{StaticResource Caption}" Margin="0,0,10,0" />
                <Slider Minimum="0" Maximum="1" Value="{Binding Threshold}" Width="150" VerticalAlignment="Center" />
                <TextBlock Text="{Binding Threshold, StringFormat={}{0:F2}}" VerticalAlignment="Center" Margin="10,0,0,0" Style="{StaticResource Body}" />
            </StackPanel>
        </Border>

        <!-- 상태줄 -->
        <Border DockPanel.Dock="Bottom" Background="{StaticResource SidebarBg}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="16,7">
            <TextBlock Text="{Binding Status}" Style="{StaticResource Caption}" />
        </Border>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="210" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- 갤러리 -->
            <DockPanel Grid.Column="0" Background="{StaticResource SidebarBg}">
                <TextBlock DockPanel.Dock="Top" Text="샘플 갤러리" Style="{StaticResource SectionLabel}" Margin="12,14,0,8" />
                <ListBox ItemsSource="{Binding Thumbnails}" SelectedItem="{Binding SelectedThumb}"
                         Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.CanContentScroll="False">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Width" Value="72" />
                            <Setter Property="Margin" Value="3,3" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="b" Background="{StaticResource CardBg}" CornerRadius="5"
                                                BorderBrush="Transparent" BorderThickness="2" Padding="3">
                                            <Image Source="{Binding Image}" Stretch="Uniform" Height="54" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource BorderBrush}" />
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource Accent}" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </DockPanel>

            <!-- 원본 + 오버레이 (휠=줌, 드래그=팬) -->
            <Border Grid.Column="1" Background="#FF1E1E1E" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1,0,1,0" ClipToBounds="True">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="원본 + 마스크 오버레이" Style="{StaticResource SectionLabel}" Margin="14,12,0,8" />
                    <Grid Margin="14,0,14,14" Background="Transparent" local:ZoomPan.Enabled="True">
                        <Image Source="{Binding OriginalImage}" Stretch="Uniform" />
                        <Image Source="{Binding OverlayImage}" Stretch="Uniform"
                               Visibility="{Binding ShowOverlay, Converter={StaticResource BoolToVis}}" />
                    </Grid>
                </DockPanel>
            </Border>

            <!-- 확률 맵 -->
            <Border Grid.Column="2" Background="#FF1E1E1E">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="확률 맵 (Probability Map)" Style="{StaticResource SectionLabel}" Margin="14,12,0,8" />
                    <Image Source="{Binding ProbabilityImage}" Stretch="Uniform" Margin="14,0,14,14" />
                </DockPanel>
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
